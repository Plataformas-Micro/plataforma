name: store

services:

  db:
    image: postgres:17
    ports:
      - 5432:5432
    volumes:
      - ${VOLUME_DB}:/var/lib/postgresql/data
    environment:
      POSTGRES_USER: ${DB_USER:-store}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-devpass}
      POSTGRES_DB: ${DB_NAME:-store}

  account:
    build:
      context: ./account-service
      dockerfile: Dockerfile
      envairoment:
        DATABASE_HOST: db
        DATABASE_PORT: 5432
        DATABASE_NAME: ${DB_NAME:-store}
        DATABASE_USER: ${DB_USER:-store}
        DATABASE_PASSWORD: ${DB_PASSWORD:-devpass}
    ports:
      - 8080:8080
    depends_on:
      - db